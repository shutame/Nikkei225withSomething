syntax = "proto3";

enum range {
    DAY = 0;
    WEEK = 1;
    MONTH = 2;
    YEAR = 3;
}

message NikkeiWithSomething {
    uint64 beginTime = 1;
    uint64 endTime = 2;
    range range = 3;

    repeated uint64 dots = 4;
    string currency = 5;

    map<string, string> something = 6;
}